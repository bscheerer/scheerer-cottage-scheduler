import PhaseStub from "../components/PhaseStub";

export default function ApprovalQueue() {
  return (
    <PhaseStub
      phase="Phase 3"
      title="Approval queue"
      blurb="Admin & Super User only. In Phase 3 this becomes the live queue of pending requests with one-click approve / deny, conflict detection on approval, and email notifications back to the requester."
    />
  );
}
