import PhaseStub from "../components/PhaseStub";

export default function Calendar() {
  return (
    <PhaseStub
      phase="Phase 2"
      title="Monthly & weekly calendar"
      blurb="The default monthly view (with one-click weekly and agenda views) lands in Phase 2. Approved reservations render as continuous chips; pending requests show in warm sunset; today is ringed in soft aqua. The calendar reads from the Reservation and Request tables defined in this Phase 1 backend."
    />
  );
}
