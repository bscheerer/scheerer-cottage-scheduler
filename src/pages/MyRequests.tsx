import PhaseStub from "../components/PhaseStub";

export default function MyRequests() {
  return (
    <PhaseStub
      phase="Phase 3"
      title="My requests"
      blurb="Once Phase 3 ships, this page lists every date request you've submitted with its status (pending / approved / denied / cancelled). You'll be able to cancel pending requests and re-request denied dates with one click."
    />
  );
}
